<h2>Location: <%= @location.display_name %> </h1>

<table>
    <tr>
        <th>Latitude: </th>
        <td> <%= @location.lat %>
    </tr>
    <tr>
        <th>Longitude: </th>
        <td> <%= @location.long %>
    </tr>
</table>

<p>Last updated: <%= @location.updated_at %> </p>

<%= button_to 'Get Forecast', location_forecast_path, method:'get' %>